<% @posts.each do |post| %>
  <%= render partial: "post", locals: { post: post } %>
  <%# 右のpostはeach内の変数としてのpostで、postのインスタンスを生成している、左は部分テンプレート内の変数 %>
  <%# partialは部分テンプレートファイルの大元である_postを呼び出している。 %>
<% end %>


  
